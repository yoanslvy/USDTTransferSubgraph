type Exchange @entity {
  # Transaction hash
  id: ID!

  # sender of token
  transferFrom: String!

  # receiver of token
  transferTo: String!

  # block number of transaction
  blockNumber: BigInt!

  # amount transfered
  amount: BigInt!
}
